name: Security Scanning

on: [pull_request]

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
    - name: Run Bridgecrew 
      id: Bridgecrew
      uses: bridecrewio/bridgecrew-action@master
      with:
        api-key: ${{ secrets.BRIDGECREW_API_KEY }}
